import React from 'react'
import ReactDom from 'react-dom'

ReactDom.render(<h1>Hola desde elemento</h1>, document.getElementById('app'))

/*

const root = createRoot(document.getElementById('app'));

root.render(
    <React.StrictMode>
        div
    </React.StrictMode>
);

reportWebVitals();

 */
